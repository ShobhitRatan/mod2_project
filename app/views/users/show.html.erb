    <h1>Show User</h1> 

    <h3><%= @user.name %></h3> 

    <h3><%= @user.email %></h3> 

    <% @user.plants.uniq.each do |plant| %> 
        <p><%= link_to plant.name, plant_path(plant) %></p> 
    <% end %> 

    <% @user.tools.uniq.each do |tool| %> 
        <p><%= link_to tool.name, tool_path(tool) %></p> 
    <% end %> 

    <h2><%= button_to "Back", users_path, method: :get %></h2> 
    <h2><%= button_to "Edit", edit_user_path(@user), method: :get %></h2> 
    <%= button_to 'Delete', plants_path, method: :delete, data: { confirm: 'Are you sure you want to delete this user?' } %> 

</div>

